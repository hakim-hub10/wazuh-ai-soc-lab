<group name="local,sshd,">

  <!-- Brute force -->
  <rule id="100001" level="10" frequency="5" timeframe="120">
    <if_matched_sid>5710</if_matched_sid>
    <description>SSH brute force-attack detekterad</description>
  </rule>

  <!-- Success efter brute force -->
  <rule id="100002" level="12">
    <if_matched_sid>5715</if_matched_sid>
    <same_source_ip/>
    <description>Lyckad SSH efter attacker</description>
  </rule>

</group>
